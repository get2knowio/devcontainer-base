{
  // Canonical devcontainer.json lives under .devcontainer/ to satisfy devcontainers/ci and CLI expectations.
  // Dockerfile stays one directory up to keep it adjacent to related build assets.
  "name": "Unified DevContainer (Python + TypeScript + Rust)",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
    "args": {
      "BUILDKIT_INLINE_CACHE": "1"
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/aws-cli:1": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "moby": false,
      "azureDnsAutoDetection": true,
      "installDockerBuildx": true,
      "version": "latest",
      "dockerDashComposeVersion": "v2"
    },
    "ghcr.io/devcontainers/features/rust:1": {
      "version": "latest",
      "profile": "default",
      "components": "rust-analyzer,rust-src,rustfmt,clippy"
    },
    "ghcr.io/eitsupi/devcontainer-features/jq-likes:2": {},
    "ghcr.io/jsburckhardt/devcontainer-features/uv:1": {}
  },
  "runArgs": ["--privileged"],
  "overrideCommand": false,
  "init": true,
  "privileged": true,
  "remoteUser": "vscode",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },
  "postCreateCommand": "poetry --version && node --version && npm --version && (tsc --version || npx tsc --version) && rustc --version && cargo --version"
}
